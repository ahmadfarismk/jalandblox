# Settings for the live site and preview links (`npm run build`).
# Committed on purpose: nothing secret goes in this file.
#
# Real GPS and real check-in, but reviews still use the fake sender until
# task F10 is done. Then change this to: VITE_USE_MOCKS=false
VITE_USE_MOCKS=api
